name: Build Multi-Platform Executables

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11']
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build executable (Linux)
        run: |
          pyinstaller --onefile --noconsole build.spec

      - name: Create distribution package
        run: |
          mkdir -p dist/Cell_Infiltrations_linux_py${{ matrix.python-version }}
          cp dist/Cell\ Infiltrations dist/Cell_Infiltrations_linux_py${{ matrix.python-version }}/
          cd dist
          tar -czf Cell_Infiltrations_linux_py${{ matrix.python-version }}.tar.gz Cell_Infiltrations_linux_py${{ matrix.python-version }}/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Cell_Infiltrations_linux_py${{ matrix.python-version }}
          path: dist/Cell_Infiltrations_linux_py${{ matrix.python-version }}.tar.gz
          retention-days: 30
          compression-level: 9

  build-macos-intel:
    runs-on: macos-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11']
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build executable (macOS Intel)
        run: |
          pyinstaller --onefile --noconsole build.spec

      - name: Create distribution package
        run: |
          mkdir -p dist/Cell_Infiltrations_macos_x86_64_py${{ matrix.python-version }}
          cp dist/Cell\ Infiltrations dist/Cell_Infiltrations_macos_x86_64_py${{ matrix.python-version }}/
          cd dist
          zip -r Cell_Infiltrations_macos_x86_64_py${{ matrix.python-version }}.zip Cell_Infiltrations_macos_x86_64_py${{ matrix.python-version }}/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Cell_Infiltrations_macos_x86_64_py${{ matrix.python-version }}
          path: dist/Cell_Infiltrations_macos_x86_64_py${{ matrix.python-version }}.zip
          retention-days: 30
          compression-level: 9

  build-macos-arm64:
    runs-on: macos-14
    strategy:
      matrix:
        python-version: ['3.10', '3.11']
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build executable (macOS ARM64)
        run: |
          pyinstaller --onefile --noconsole build.spec

      - name: Create distribution package
        run: |
          mkdir -p dist/Cell_Infiltrations_macos_arm64_py${{ matrix.python-version }}
          cp dist/Cell\ Infiltrations dist/Cell_Infiltrations_macos_arm64_py${{ matrix.python-version }}/
          cd dist
          zip -r Cell_Infiltrations_macos_arm64_py${{ matrix.python-version }}.zip Cell_Infiltrations_macos_arm64_py${{ matrix.python-version }}/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Cell_Infiltrations_macos_arm64_py${{ matrix.python-version }}
          path: dist/Cell_Infiltrations_macos_arm64_py${{ matrix.python-version }}.zip
          retention-days: 30
          compression-level: 9

  build-windows:
    runs-on: windows-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11']
        arch: ['x86', 'x64']
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
          architecture: ${{ matrix.arch == 'x64' && 'x64' || 'x86' }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build executable (Windows)
        run: |
          pyinstaller --onefile --noconsole build.spec

      - name: Create distribution package
        shell: powershell
        run: |
          New-Item -ItemType Directory -Path "dist\Cell_Infiltrations_windows_${{ matrix.arch }}_py${{ matrix.python-version }}" -Force
          Copy-Item "dist\Cell Infiltrations.exe" "dist\Cell_Infiltrations_windows_${{ matrix.arch }}_py${{ matrix.python-version }}\"
          cd dist
          Compress-Archive -Path "Cell_Infiltrations_windows_${{ matrix.arch }}_py${{ matrix.python-version }}" -DestinationPath "Cell_Infiltrations_windows_${{ matrix.arch }}_py${{ matrix.python-version }}.zip"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Cell_Infiltrations_windows_${{ matrix.arch }}_py${{ matrix.python-version }}
          path: dist/Cell_Infiltrations_windows_${{ matrix.arch }}_py${{ matrix.python-version }}.zip
          retention-days: 30
          compression-level: 9

  create-release:
    needs: [build-linux, build-macos-intel, build-macos-arm64, build-windows]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: false

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: artifacts/**/*
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
